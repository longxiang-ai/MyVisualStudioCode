我们为每个关系定义触发器，这些关系的主键由某个其他关系的外键引用。 每当从引用关系中删除元组时，将激活触发器。 触发器执行的操作将是访问所有引用关系，并删除其中的所有元组，这些元组的外键属性值与所引用关系中已删除的元组的主键属性值相同 。 这些触发器集将处理on Delete级联操作。
we can define triggers for the relation whose primary key are referred by the foreign key of others’.
When deleting a tuple , the trigger will be activated. then the trigger will access all the referring relations whose foreign key is the same as primary key, do the delete operation as well. then the " on delete cascade " operation finished.